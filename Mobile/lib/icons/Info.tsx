import { Info, Camera, Plus } from 'lucide-react-native';
import { iconWithClassName } from './iconWithClassName';
iconWithClassName(Info);
iconWithClassName(Camera);
iconWithClassName(Plus);

export { Info, Camera, Plus};